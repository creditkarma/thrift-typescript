{
  "name": "@creditkarma/thrift-typescript",
  "version": "0.1.0",
  "description": "Generate TypeScript from Thrift IDL files",
  "main": "./dist/app/index.js",
  "scripts": {
    "clean": "rimraf coverage/ dist/ node_modules/",
    "build": "rimraf dist/; tsc",
    "postbuild": "rimraf dist/tests",
    "build:watch": "tsc --watch",
    "lint": "tslint --fix src/**/*",
    "test": "npm run lint && npm run test:only --",
    "test:integration": "ts-node ./src/tests/integration/index.ts",
    "test:watch": "npm test -- -w",
    "test:only": "NODE_ENV=test mocha --opts mocha.opts",
    "coverage": "NODE_ENV=test nyc mocha --opts mocha.cover.opts"
  },
  "bin": {
    "thrift-typescript": "./dist/app/bin/index.js"
  },
  "keywords": [
    "Thrift",
    "TypeScript"
  ],
  "author": "Credit Karma",
  "license": "Apache-2.0",
  "dependencies": {
    "typescript": "^2.4.2"
  },
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/express": "^4.0.37",
    "@types/mocha": "^2.2.32",
    "@types/node": "^7.0.22",
    "@types/rimraf": "0.0.28",
    "chai": "^3.5.0",
    "express": "^4.15.4",
    "mocha": "^2.5.3",
    "nyc": "^11.1.0",
    "rimraf": "^2.5.4",
    "source-map-support": "^0.4.0",
    "thrift": "^0.10.0",
    "ts-node": "^3.3.0",
    "tslint": "^5.3.2"
  },
  "types": "dist/index.d.ts",
  "nyc": {
    "include": [
      "src/*.ts",
      "src/**/*.ts"
    ],
    "exclude": [
      "src/*.test.ts",
      "src/**/*.test.ts"
    ],
    "extension": [
      ".ts"
    ],
    "reporter": [
      "lcov",
      "json"
    ],
    "all": true
  }
}
